# راه‌اندازی «ورود با موبایل و پیامک»

ورود مشتری روی سایت آماده است، ولی برای کار کردن به سه چیز نیاز دارد که هیچ‌کدام
داخل کد نیستند و باید برای هر سایتِ جدید یک‌بار ساخته شوند:

| چیز | برای چه | هزینه |
|---|---|---|
| پایگاه داده (Neon) | نگه‌داشتن کدهای ورود و کاربران (و همچنین سفارش‌ها و پیگیری) | پلن رایگان کافی است |
| پنل پیامک (کاوه‌نگار) | فرستادن رمز یکبار مصرف به موبایل | اعتبار پیامک (هر پیامک هزینه دارد) |
| `AUTH_SECRET` | امضای کوکی ورود | رایگان |

مراحل را به همین ترتیب انجام دهید. (اسم بعضی منوها ممکن است کمی فرق کرده باشد.)

---

## مرحله ۱ — ساخت دیتابیس (Neon)

1. وارد [vercel.com](https://vercel.com) شوید و پروژه‌ی سایت را باز کنید.
2. تب **Storage** ← **Create Database** ← **Neon** (Postgres) را انتخاب کنید.
3. پلن رایگان را بردارید. منطقه (Region) را نزدیک به Washington D.C. / US East بگذارید
   (منطقه‌ی پیش‌فرض توابع Vercel است و سرعت بیشتری می‌دهد).
4. در پایان از شما می‌خواهد دیتابیس را به پروژه وصل کنید (**Connect Project**) — تأیید کنید.
   Vercel خودش متغیر `DATABASE_URL` را در تنظیمات پروژه می‌سازد.
5. روی **Open in Neon** بزنید ← **SQL Editor**.
6. تمام محتوای فایل `lib/schema.sql` (همین پروژه) را کپی و در SQL Editor بچسبانید ← **Run**.
   با این کار جدول‌های سفارش‌ها و ورود ساخته می‌شود. اجرای دوباره‌اش بی‌خطر است.

> اگر ترجیح می‌دهید بدون Vercel: در [neon.tech](https://neon.tech) ثبت‌نام کنید، یک پروژه بسازید،
> «Connection string» را کپی کنید و خودتان به‌عنوان `DATABASE_URL` در Vercel بگذارید.

## مرحله ۲ — پنل پیامک (کاوه‌نگار)

1. در [kavenegar.com](https://kavenegar.com) ثبت‌نام کنید و احراز هویت حساب را کامل کنید
   (برای ارسال پیامک لازم است).
2. حساب را شارژ کنید (بخش شارژ / خرید اعتبار).
3. **کلید API** را از بخش تنظیمات حساب بردارید (رشته‌ی طولانی؛ محرمانه است).
4. یک **قالب (الگو / ارسال کد تأیید)** بسازید:
   - نام: مثلاً `gorg-otp`
   - متن دقیقاً: `کد ورود شما به رستوران گرگ: %token`
   - منتظر تأیید قالب بمانید (از چند دقیقه تا حدود یک روز).

## مرحله ۳ — متغیرهای محیطی در Vercel

1. یک رشته‌ی تصادفی برای `AUTH_SECRET` بسازید. در ترمینال (Node نصب است):
   ```
   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
   ```
2. در Vercel: پروژه ← **Settings** ← **Environment Variables** و این‌ها را اضافه کنید
   (برای Production، و ترجیحاً Preview هم):

   | نام | مقدار |
   |---|---|
   | `AUTH_SECRET` | رشته‌ی مرحله‌ی قبل |
   | `KAVENEGAR_API_KEY` | کلید API کاوه‌نگار |
   | `KAVENEGAR_OTP_TEMPLATE` | نام قالب، مثلاً `gorg-otp` |
   | `DATABASE_URL` | خودکار از مرحله‌ی ۱ ساخته شده است |

3. تغییر متغیرها فقط روی دیپلوی‌های جدید اثر می‌گذارد: **Deployments** ← ⋯ ← **Redeploy**.

## مرحله ۴ — تست

روی سایت: **ورود** ← شماره‌ی خودتان ← باید پیامک برسد ← کد را بزنید.

تست بدون پیامک واقعی (روی کامپیوتر خودتان): فایل `.env.local` بسازید با
`DATABASE_URL` و `AUTH_SECRET` (`KAVENEGAR_API_KEY` را خالی بگذارید)، بعد `npm run dev`.
کد ورود در ترمینال چاپ می‌شود. (این حالت فقط در `npm run dev` کار می‌کند، هرگز روی سایت واقعی.)

## اگر کار نکرد

| پیام روی سایت | علت محتمل |
|---|---|
| «ورود با پیامک هنوز راه‌اندازی نشده» | `AUTH_SECRET` یا `DATABASE_URL` تنظیم نشده، یا بعد از تنظیم Redeploy نکرده‌اید |
| «خطا در ارسال کد» | جدول‌ها ساخته نشده‌اند (`lib/schema.sql` را اجرا کنید). جزئیات: Vercel ← Logs |
| «ارسال پیامک با مشکل روبه‌رو شد» | کلید API اشتباه، اعتبار کم، قالب هنوز تأیید نشده یا نامش با `KAVENEGAR_OTP_TEMPLATE` یکی نیست. در Vercel ← Logs خطای کاوه‌نگار (با کد عددی) دیده می‌شود |

---

## فروش سایت به مشتری

سایت (کد) را می‌شود فروخت؛ **حساب‌ها و کلیدهای شما را نه.** بهتر است خریدار این سه چیز را
با حساب و هویت خودش بسازد:

- **کاوه‌نگار:** کلید و اعتبار به هویتِ صاحب حساب وصل است. اگر کلید شما روی سایت او بماند،
  هزینه‌ی همه‌ی پیامک‌ها را شما می‌پردازید و مسئولِ پیامک‌هایی هستید که از خط شما می‌رود.
- **دیتابیس:** شماره‌ی مشتری‌ها و سفارش‌ها مالِ رستوران است و نباید در حساب توسعه‌دهنده بماند.
- **Vercel / دامنه:** پروژه را به حساب خریدار منتقل کنید یا او خودش پروژه بسازد.

پیشنهاد عملی: سایت + همین راهنما را تحویل بدهید و یا خودتان با حساب‌های خودِ خریدار
(و با حضور او) مراحل ۱ تا ۳ را انجام دهید. برای دمو می‌توانید موقتاً از حساب خودتان
استفاده کنید ولی قبل از تحویل، کلیدها را از Vercel پاک کنید و در پنل کاوه‌نگار کلید را بازسازی کنید.

اگر خریدار سرویس پیامک دیگری (ملی‌پیامک، sms.ir و…) می‌خواهد، فقط تابع `sendOtpSms`
در `lib/kavenegar.ts` باید عوض شود؛ بقیه‌ی ورود دست نمی‌خورد.
